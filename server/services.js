var noamValidation 	= require('./someApi/noam');
	Q				= require('q');

module.exports.MyService = MyService;

function MyService (num) {
	// TODO: Build a service to use the functions inside noamValidation object
	// noamValidation.validate1 && noamValidation.validate2
	// noamValidation api is in noam.js
	// after you receive the validations from both functions, make sure the result is equal and return it as a Promise
}